apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-chart-4-python-metric-log-grafana.fullname" . }}-test-connection"
  labels:
    {{- include "helm-chart-4-python-metric-log-grafana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-chart-4-python-metric-log-grafana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
