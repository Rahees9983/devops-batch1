apiVersion: v1
kind: Service
metadata:
  name: myapp-green
  namespace: default
  labels:
    app: myapp
    environment: green
spec:
  type: ClusterIP
  selector:
    app: myapp
    environment: green
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
