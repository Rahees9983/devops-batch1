apiVersion: v1
kind: Service
metadata:
  name: myapp-service
  namespace: default
  labels:
    app: myapp
spec:
  type: ClusterIP
  selector:
    app: myapp
    # Note: No version selector - traffic goes to BOTH v1 and v2
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
